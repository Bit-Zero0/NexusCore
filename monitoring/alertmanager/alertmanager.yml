global:
  resolve_timeout: 5m

route:
  receiver: generic-all
  group_by: ["alertname", "severity", "broker", "queue", "lane"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - receiver: critical-wecom
      matchers:
        - severity = "critical"
    - receiver: warning-feishu
      matchers:
        - severity = "warning"

receivers:
  - name: generic-all
    webhook_configs:
      - url: http://alert-relay:18081/dry-run
      - url: http://alert-relay:18081/generic
  - name: critical-wecom
    webhook_configs:
      - url: http://alert-relay:18081/dry-run
      - url: http://alert-relay:18081/generic
      - url: http://alert-relay:18081/wecom
  - name: warning-feishu
    webhook_configs:
      - url: http://alert-relay:18081/dry-run
      - url: http://alert-relay:18081/generic
      - url: http://alert-relay:18081/feishu
